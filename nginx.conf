server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # статика отдаётся как есть
  location /assets/ {
    try_files $uri =404;
  }

  # SPA роуты
  location / {
    try_files $uri /index.html;
  }
}
